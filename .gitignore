node_modules/
dist/
# Solo el directorio de datos de runtime, anclado a la raíz. Un patrón "data/"
# sin barra inicial también captura src/data/, que es donde vive trip.js con
# toda la ruta del viaje: el archivo no se subía y el build fallaba.
/data/
*.log
.env
.DS_Store
# El Dockerfile SÍ se versiona: es la vía de build que instala Python y
# fast-flights para que «Consultar precios» funcione en el servidor.
# (Estuvo ignorado durante el debugging; ya no.)
